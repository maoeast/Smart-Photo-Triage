# Phase H GREEN evidence

Date: 2026-08-21
Platform: Windows, Python 3.13
Scope: local synthetic release-candidate checks only.

All fixtures use temporary directories, generated bytes, a fake video backend, and offline Fake AI.
No real media, network, credential, external API, commit, or push was used.

```text
rtk python -m pytest -q -s tests/test_phase_h_release.py
2 passed in 58.95s
PHASE_H_1K_FULL_LIFECYCLE items=1000 elapsed=51.518s scan_items_per_second=775.8
preview_items_per_second=41.6 peak_rss_mib=69.85546875 sqlite_mib=6.77 cache_hits=1000
groups=1/1 failed=0 comparisons=12986 fake_requests=10
```

T-H-001 through T-H-005 prove source immutability, target SHA-256s, HUMAN plan precedence,
HEIC/MOV/AAE target linkage, and isolated corrupt media failures. H1 names exact duplicates,
same-size different bytes, four-item burst, false-chain controls, missing EXIF, two videos, and a
Windows reserved-name case. The second H2 pass has stable scan rows and canonical plan, no targets,
and Fake AI request_count=0. H4 runs scan, preprocess, group, Fake AI, plan, approval, preflight,
dry-run, then cache-only second passes. Its 12,986 comparisons are below the 32,000 linear bound.
Peak RSS is Windows peak working set.

## Clean base install and CLI

`python -m venv`, `pip install .`, `spt --help`, `spt init`, and `spt doctor` passed in a fresh
temporary environment. It installed `Pillow-12.3.0` and `smart-photo-triage-0.1.0`; doctor returned
`{"diagnoses": [], "lock_status": "NONE", "ok": true}`. The temporary environment was removed.
Pillow is the sole declared runtime dependency, used by metadata/preprocess, with metadata license
expression `MIT-CMU`. Optional `pillow-heif` is not installed by the base package; no unused declared
runtime dependency was found.

## Boundaries

This is not `READY_FOR_FINAL_ACCEPTANCE`. The later full-suite and coverage closure is recorded
below; Windows/Linux remote CI and optional real-provider smoke remain unrun.

## Final local coverage closure

```text
rtk python -m pytest -q
483 passed, 7 skipped in 220.95s

rtk python -m pytest -q --cov=smart_photo_triage --cov-branch --cov-report=term
483 passed, 7 skipped in 263.39s; total 90.09%

planner focused: tests/test_phase_f_planner.py + tests/test_phase_g_executor.py
194 collected; planner statements 513/542 = 94.649%; branches 148/164 = 90.244%

executor focused: tests/test_phase_g_executor.py
151 passed in 27.41s; executor statements 1386/1431 = 96.855%; branches 461/484 = 95.248%

rtk python -m ruff format --check .
71 files already formatted
rtk python -m ruff check .
All checks passed!
```

The four extra planner outcomes are synthetic and observable: empty Windows-component fallback,
low-confidence capture treated as unreliable, pending approval rejected as current authority, and
a regular-file output root rejected. They close the independently measured branch gate without
excluding code or changing product behavior.
